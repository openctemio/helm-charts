apiVersion: v1
kind: Service
metadata:
  name: {{ include "openctem.fullname" . }}
  labels:
    {{- include "openctem.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openctem.selectorLabels" . | nindent 4 }}

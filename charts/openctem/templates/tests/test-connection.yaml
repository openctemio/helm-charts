apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "openctem.fullname" . }}-test-connection"
  labels:
    {{- include "openctem.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "openctem.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
